<?php declare(strict_types=1);

/** @var array<string,int|null> $data */
if (!$data) /* pos 2:1 */ return;
echo '## Warnings

';
foreach ($data as $item => $count) /* pos 5:1 */ {
	[$file, $line, $message] = explode('|', $item, 3) /* pos 6:2 */;
	echo '- ';
	echo Tracy\Helpers::escapeMd($message . ' in ' . $file . ':' . $line . ($count > 1 ? ' (×' . $count . ')' : '')) /* pos 7:4 */;
	echo "\n";

}
